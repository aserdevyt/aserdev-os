### BASIC NAVIGATION ###
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias ~="cd ~"
alias c="clear"
alias cl="clear; ls"

### LS ON STEROIDS ###
alias ls="ls --color=auto -F"
alias ll="ls -lh --color=auto"
alias la="ls -A --color=auto"
alias lla="ls -lah --color=auto"

### GIT (so you don’t lose sanity) ###
alias gs="git status"
alias ga="git add ."
alias gc="git commit -m"
alias gp="git push"
alias gpl="git pull"
alias gco="git checkout"
alias gcm="git commit -m"
alias gundo="git reset --soft HEAD~1"   # undo last commit
alias gcl="git clone"

### PACMAN / ARCH STUFF ###
alias update="sudo pacman -Syu --noconfirm"
alias search="pacman -Ss"
alias install="sudo pacman -Syu --noconfirm"
alias remove="sudo pacman -Rns"
alias clean="sudo pacman -Rns \$(pacman -Qtdq)"   # yeet orphans
alias pkginfo="pacman -Qi"

### YAY (if you’re on AUR grind) ###
alias yupdate="yay -Syu --devel --timeupdate --noconfirm"
alias yinstall="yay -S --noconfirm"
alias yremove="yay -Rns"

### RANDOM QUALITY OF LIFE ###
alias please="sudo !!"       # save your life when you forget sudo
alias fuck="sudo !!"         # same but meme edition
alias e="exit"
alias v="nvim"
alias cat="bat --style=plain --paging=never"  # if you got bat
alias grep="grep --color=auto"

### SHORTCUTS ###
alias ip="ip -c a"
alias ports="ss -tulwn"
alias myip="curl ifconfig.me"
alias htop="btop"    # if you’re based
alias psmem="ps auxf | sort -nr -k 4 | head -10"
alias pscpu="ps auxf | sort -nr -k 3 | head -10"

### MEME COMMANDS ###
alias pleasework="make && make install"
alias pls="echo 'no 💀'"
alias yk="echo 'you know the vibes ✨'"

# Replace vim with sudo nvim
alias vim="nvim"

# Open grub config in nvim, then confirm before updating grub
function editgrub() {
    sudo nvim /etc/default/grub

    echo -n "Did you actually edit it right tho? (y/N) 👉 "
    read -r ans
    if [[ $ans == "y" || $ans == "Y" ]]; then
        echo "Updating grub... ⚙️"
        sudo grub-mkconfig -o /boot/grub/grub.cfg
        echo "Done ✅ grub updated"
    else
        echo "Ok fine 💀 grub not touched"
    fi
}
alias grubedit="editgrub"

ytdl480() {
    if [[ $# -eq 0 ]]; then
        echo "Usage: ytdl480 <youtube-url> [<youtube-url> ...]"
        return 1
    fi

    for url in "$@"; do
        yt-dlp \
            -f "bestvideo[height<=480]+bestaudio/best[height<=480]" \
            -o "%(uploader)s/%(uploader)s-%(title)s+%(upload_date)s+%(id)s.%(ext)s" \
            --download-archive "$HOME/.yt-dlp-archive.txt" \
            "$url"
    done
}


alias ffconv='~/convert.sh'

alias rm="rm -rf"
alias mv="mv -f"
alias cp="cp -f"
